# Environment files live next to the app that reads them:
#
#   backend/.env.example   -> copy to backend/.env
#   frontend/.env.example  -> copy to frontend/.env
#
# See DEV-TH.md for running the dashboard on your own machine,
# and deploy/README.md for the VPS.
